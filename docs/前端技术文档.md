# AI 低代码平台 - 前端技术文档

## 1. 项目概述

前端采用 React 19 + TypeScript + Vite 技术栈，实现了完整的 AI 低代码平台功能，包括工作流可视化、AI 对话、知识库管理、模型管理等。

## 2. 技术栈详解

### 2.1 核心依赖

| 技术/库 | 版本 | 用途 |
|----------|------|------|
| React | 19.2.4 | 核心 UI 框架 |
| TypeScript | 6.0.2 | 类型安全 |
| Vite | 8.0.4 | 构建工具 |
| React Router | 7.14.0 | 路由管理 |
| Zustand | 5.0.12 | 状态管理 |
| React Flow | 11.11.4 | 工作流可视化 |
| TailwindCSS | 4.2.2 | CSS 框架 |
| ShadCN UI | 4.2.0 | UI 组件库 |
| TanStack Query | 5.99.2 | 数据请求 |
| Axios | 1.15.0 | HTTP 客户端 |
| Zod | 4.3.6 | 数据验证 |

### 2.2 开发依赖

| 技术/库 | 版本 | 用途 |
|----------|------|------|
| Vitest | 2.1.8 | 测试框架 |
| ESLint | 9.39.4 | 代码检查 |

## 3. 项目结构

```
web/
├── public/
│   ├── favicon.svg
│   └── icons.svg
├── src/
│   ├── components/
│   │   ├── api-keys/
│   │   │   └── ApiKeysManager.tsx
│   │   ├── apps/
│   │   │   ├── AppCard.tsx
│   │   │   └── EditAppDialog.tsx
│   │   ├── builder/
│   │   │   ├── nodes/
│   │   │   │   ├── BaseNode.tsx
│   │   │   │   ├── ConditionNode.tsx
│   │   │   │   ├── EndNode.tsx
│   │   │   │   ├── KnowledgeBaseNode.tsx
│   │   │   │   ├── LlmNode.tsx
│   │   │   │   ├── StartNode.tsx
│   │   │   │   ├── SystemPromptNode.tsx
│   │   │   │   ├── UserInputNode.tsx
│   │   │   │   ├── VariableSetNode.tsx
│   │   │   │   ├── index.ts
│   │   │   │   └── types.ts
│   │   │   ├── panels/
│   │   │   │   └── NodePanel.tsx
│   │   │   ├── BuilderCanvas.tsx
│   │   │   ├── EdgeContextMenu.tsx
│   │   │   ├── NodeContextMenu.tsx
│   │   │   ├── NodePropertiesDialog.tsx
│   │   │   └── PreviewDialog.tsx
│   │   ├── chat/
│   │   │   ├── ConversationList.tsx
│   │   │   ├── MessageInput.tsx
│   │   │   ├── MessageList.tsx
│   │   │   └── MessageSkeleton.tsx
│   │   ├── layout/
│   │   │   ├── Header.tsx
│   │   │   ├── MainLayout.tsx
│   │   │   └── Sidebar.tsx
│   │   ├── models/
│   │   │   └── ModelFormDialog.tsx
│   │   ├── ui/
│   │   │   ├── avatar.tsx
│   │   │   ├── button.tsx
│   │   │   ├── card.tsx
│   │   │   ├── dialog.tsx
│   │   │   ├── dropdown-menu.tsx
│   │   │   ├── field.tsx
│   │   │   ├── input.tsx
│   │   │   ├── label.tsx
│   │   │   ├── select.tsx
│   │   │   ├── separator.tsx
│   │   │   ├── skeleton.tsx
│   │   │   ├── sonner.tsx
│   │   │   ├── tabs.tsx
│   │   │   └── textarea.tsx
│   │   └── ProtectedRoute.tsx
│   ├── hooks/
│   │   ├── useApi.ts
│   │   └── useKeyPress.ts
│   ├── lib/
│   │   ├── api-client.ts
│   │   ├── axios.ts
│   │   └── utils.ts
│   ├── pages/
│   │   ├── Apps.tsx
│   │   ├── Builder.tsx
│   │   ├── Chat.tsx
│   │   ├── Knowledge.tsx
│   │   ├── KnowledgeDetail.tsx
│   │   ├── Login.tsx
│   │   ├── ModelManagement.tsx
│   │   ├── Register.tsx
│   │   └── Share.tsx
│   ├── store/
│   │   ├── appStore.ts
│   │   ├── builderStore.ts
│   │   └── userStore.ts
│   ├── types/
│   │   ├── api.types.ts
│   │   └── index.ts
│   ├── App.css
│   ├── App.tsx
│   ├── AppRoutes.tsx
│   ├── index.css
│   └── main.tsx
├── .gitignore
├── README.md
├── components.json
├── eslint.config.js
├── index.html
├── package.json
├── pnpm-lock.yaml
├── tsconfig.app.json
├── tsconfig.json
├── tsconfig.node.json
├── vite.config.ts
└── vitest.config.ts
```

## 4. 架构设计

### 4.1 应用入口 (`main.tsx`)

```typescript
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import App from './App.tsx'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
```

### 4.2 根组件 (`App.tsx`)

```typescript
import { BrowserRouter as Router } from "react-router-dom";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { useEffect } from "react";
import AppRoutes from "./AppRoutes";
import { Toaster } from "@/components/ui/sonner";
import { useAppStore } from "./store/appStore";

const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      refetchOnWindowFocus: false,
      retry: 1,
      staleTime: 60 * 1000,
    },
  },
});

const AppInitializer = () => {
  const fetchApps = useAppStore((state) => state.fetchApps);
  const fetchModels = useAppStore((state) => state.fetchModels);
  const fetchKnowledgeBases = useAppStore((state) => state.fetchKnowledgeBases);
  const fetchConversations = useAppStore((state) => state.fetchConversations);

  useEffect(() => {
    // 全局初始化数据
    fetchApps();
    fetchModels();
    fetchKnowledgeBases();
    fetchConversations();
  }, [fetchApps, fetchModels, fetchKnowledgeBases, fetchConversations]);

  return null;
};

function App() {
  return (
    <QueryClientProvider client={queryClient}>
      <Router>
        <AppInitializer />
        <AppRoutes />
        <Toaster />
      </Router>
    </QueryClientProvider>
  );
}
```

**核心功能：
- QueryClientProvider: 提供 TanStack Query 上下文
- Router: React Router 路由
- AppInitializer: 全局数据初始化
- Toaster: 全局通知提示

### 4.3 路由配置 (`AppRoutes.tsx`)

| 路由 | 组件 | 说明 |
|------|------|------|
| /login | Login | 登录页面 |
| /register | Register | 注册页面 |
| /chat | Chat | 聊天界面 |
| /apps | Apps | 应用列表 |
| /apps/:appId/builder | Builder | 工作流编辑器 |
| /models | ModelManagement | 模型管理 |
| /knowledge | Knowledge | 知识库列表 |
| /knowledge/:id | KnowledgeDetail | 知识库详情 |
| /share/:shareId | Share | 分享页面 |

**路由保护：使用 ProtectedRoute 组件保护需要登录的页面。

## 5. 状态管理详解

### 5.1 appStore (应用状态)

`appStore.ts` 使用 Zustand 管理核心应用状态。

**状态结构：

| 状态 | 类型 | 说明 |
|------|------|------|
| apps | App[] | 应用列表 |
| currentApp | App \| null | 当前选中应用 |
| conversations | Conversation[] | 会话列表 |
| currentConversation | Conversation \| null | 当前会话 |
| messages | Message[] | 消息列表 |
| models | Model[] | 模型列表 |
| knowledgeBases | KnowledgeBase[] | 知识库列表 |
| currentKnowledgeBase | KnowledgeBase \| null | 当前知识库 |
| isLoading | boolean | 加载状态 |
| isLoadingMessages | boolean | 消息加载状态 |
| isSending | boolean | 发送中状态 |
| activeConversations | Map | 活跃会话 |
| initialized | object | 初始化标志 |

**核心 Actions：

```typescript
// Apps
fetchApps(force?: boolean): Promise<void>
createApp(app: Partial<App>): Promise<App>
updateApp(id: string, app: Partial<App>): Promise<void>
deleteApp(id: string): Promise<void>
setCurrentApp(app: App | null): void

// Conversations
fetchConversations(force?: boolean): Promise<void>
createConversation(appId: string, title?: string): Promise<Conversation>
updateConversation(id: string, data: { title: string }): Promise<void>
deleteConversation(id: string): Promise<void>
setCurrentConversation(conversation: Conversation | null): void

// Messages
fetchMessages(conversationId: string): Promise<void>
sendMessage(content: string): Promise<void>
sendMessageStream(content: string): Promise<void>

// Models
fetchModels(force?: boolean): Promise<void>

// Knowledge Bases
fetchKnowledgeBases(force?: boolean): Promise<void>
fetchKnowledgeBaseById(id: string): Promise<void>
createKnowledgeBase(name: string, description?: string): Promise<void>
deleteKnowledgeBase(id: string): Promise<void>
uploadDocument(knowledgeBaseId: string, file: File): Promise<void>
deleteDocument(documentId: string): Promise<void>
```

**流式响应处理：

```typescript
sendMessageStream: async (content) => {
  const { currentConversation, messages } = get();
  if (!currentConversation) return;

  const isFirstMessage = messages.length === 0;
  set((state) => {
    state.isSending = true;
    const activeConversations = new Map(state.activeConversations);
    activeConversations.set(currentConversation.id, {
      isSending: true,
      messages: messages,
    });
    return { isSending: true, activeConversations };
  });

  const userMessage: Message = {
    id: Date.now().toString(),
    role: "user",
    content,
    conversationId: currentConversation.id,
    createdAt: new Date().toISOString(),
  };

  set((state) => ({
    messages: [...state.messages, userMessage],
  }));

  try {
    const token = localStorage.getItem("token");
    const response = await fetch(
      `/api/conversations/${currentConversation.id}/messages-stream`,
      {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          Authorization: `Bearer ${token}`,
        },
        body: JSON.stringify({ content }),
      },
    );

    const reader = response.body?.getReader();
    const decoder = new TextDecoder();
    let fullContent = "";

    const tempMessageId = Date.now().toString();
    const tempMessage: Message = {
      id: tempMessageId,
      role: "assistant",
      content: "",
      conversationId: currentConversation.id,
      createdAt: new Date().toISOString(),
    };

    set((state) => ({
      messages: [...state.messages, tempMessage],
    }));

    if (reader) {
      while (true) {
        const { done, value } = await reader.read();

        if (done) {
          break;
        }

        const chunk = decoder.decode(value, { stream: true });

        const lines = chunk.split("\n");

        let doneReceived = false;

        for (const line of lines) {
          const trimmedLine = line.trim();
          if (!trimmedLine) continue;

          if (trimmedLine.startsWith("data: ")) {
            const data = trimmedLine.slice(6).trim();

            if (data === "[DONE]") {
              doneReceived = true;
              break;
            }

            try {
              const parsed = JSON.parse(data);
              if (parsed.content) {
                const calculateDelay = (char: string) => {
                  const baseDelay = 30;
                  const punctuationDelay = 200;
                  const sentenceEndDelay = 500;
                  const punctuationRegex = /[，。！？；：、]/;
                  const sentenceEndRegex = /[。！？]/;

                  if (sentenceEndRegex.test(char)) {
                    return baseDelay + sentenceEndDelay;
                  } else if (punctuationRegex.test(char)) {
                    return baseDelay + punctuationDelay;
                  } else {
                    return baseDelay;
                  }
                };

                let currentContent = fullContent;
                for (const char of parsed.content) {
                  currentContent += char;

                  await new Promise((resolve) =>
                    setTimeout(resolve, calculateDelay(char)),
                  );

                  set((state) => ({
                    messages: state.messages.map((msg) =>
                      msg.id === tempMessageId
                        ? { ...msg, content: currentContent }
                        : msg,
                    ),
                  }));
                }

                fullContent = currentContent;
              } else if (parsed.error) {
                console.error("流式响应错误:", parsed.error);
              }
            } catch (e) {
              console.error("解析流式响应失败:", e);
            }
          }
        }

        if (doneReceived) {
          break;
        }
      }
    }

    set((state) => {
      const updatedMessages = state.messages.map((msg) =>
        msg.id === tempMessageId ? { ...msg, content: fullContent } : msg,
      );

      state.isSending = false;
      const activeConversations = new Map(state.activeConversations);
      activeConversations.set(currentConversation.id, {
        isSending: false,
        messages: updatedMessages,
      });

      return {
        messages: updatedMessages,
        isSending: false,
        activeConversations,
      };
    });

    if (isFirstMessage && fullContent) {
      try {
        const title = content.slice(0, 20);
        await get().updateConversation(currentConversation.id, { title });
      } catch (e) {
        // 静默失败
      }
    }
  } catch (error) {
    console.error("发送消息失败:", error);
    set((state) => {
      state.isSending = false;
      const activeConversations = new Map(state.activeConversations);
      activeConversations.set(currentConversation.id, {
        isSending: false,
        messages: messages,
      });
      return { isSending: false, activeConversations };
    });
  }
},
```

### 5.2 builderStore (工作流编辑器状态)

管理 React Flow 的工作流编辑器状态，包括节点、边、选中状态等。

### 5.3 userStore (用户状态)

管理用户认证相关状态。

## 6. API 客户端 (`api-client.ts`)

封装了所有 API 请求，使用 Axios 作为 HTTP 客户端。

**主要 API 模块：

- appsApi: 应用管理
- conversationsApi: 会话管理
- modelManagementApi: 模型管理
- knowledgeApi: 知识库管理
- authApi: 认证

## 7. 工作流可视化 (React Flow)

### 7.1 节点类型

| 节点类型 | 组件 | 说明 |
|----------|------|------|
| start | StartNode | 开始节点 |
| end | EndNode | 结束节点 |
| userInput | UserInputNode | 用户输入 |
| systemPrompt | SystemPromptNode | 系统提示词 |
| llm | LlmNode | LLM 调用 |
| knowledgeBase | KnowledgeBaseNode | 知识库检索 |
| condition | ConditionNode | 条件分支 |
| variableSet | VariableSetNode | 变量设置 |

### 7.2 核心组件

**BuilderCanvas.tsx** - 工作流画布组件
**NodePanel.tsx** - 节点面板组件
**NodePropertiesDialog.tsx** - 节点属性对话框
**PreviewDialog.tsx** - 预览对话框

## 8. 开发规范

### 8.1 代码风格

- 使用 TypeScript 严格类型检查
- 使用 ESLint 进行代码检查
- 使用 Prettier 进行代码格式化

### 8.2 组件开发规范

- 组件文件统一使用 `.tsx` 扩展名
- 组件名使用 PascalCase
- Props 接口定义在组件文件开头
- 使用 `@/` 别名指向 `src/` 目录

### 8.3 状态管理规范

- 全局状态使用 Zustand
- 组件本地状态使用 React useState
- 数据请求使用 TanStack Query

### 8.4 样式规范

- 使用 TailwindCSS 进行样式开发
- 复杂样式可配合 CSS Modules 使用

## 9. 核心功能实现

### 9.1 认证流程

1. 用户登录/注册
2. 获取 JWT Token
3. Token 保存到 localStorage
4. 请求时在 Header 中携带 Token
5. ProtectedRoute 检查登录状态

### 9.2 工作流编辑

1. 从节点面板拖拽节点到画布
2. 连接节点创建边
3. 右键打开节点属性对话框
4. 保存工作流配置
5. 预览执行效果

### 9.3 知识库上传

1. 选择知识库
2. 上传文档文件
3. 异步处理文档（分块、向量化）
4. 显示处理状态

### 9.4 AI 对话

1. 选择应用和会话
2. 输入消息
3. 发送请求
4. 接收流式响应并显示
5. 保存会话历史

## 10. 性能优化

### 10.1 代码分割

- 路由级别的懒加载
- 组件级别的动态导入

### 10.2 资源预加载

- 使用 Vite 的预构建功能
- 关键资源预加载

### 10.3 图片优化

- 使用合适的图片格式
- 图片懒加载

### 10.4 状态更新优化

- 使用 Zustand 的 selector 避免不必要的重渲染
- 使用 React.memo 优化组件渲染

## 11. 本地开发

### 11.1 安装依赖

```bash
cd web
pnpm install
```

### 11.2 启动开发服务器

```bash
pnpm dev
```

默认运行在 http://localhost:5173

### 11.3 构建生产版本

```bash
pnpm build
```

### 11.4 运行测试

```bash
pnpm test
```

